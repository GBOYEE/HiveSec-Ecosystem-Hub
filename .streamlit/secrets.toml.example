# Streamlit secrets for HiveSec Hub
# Copy to .streamlit/secrets.toml and adjust values

# Database connection (optional if using local SQLite)
# database_url = "postgresql://user:pass@localhost/hivesec"

# Other secrets (future)
# slack_webhook_url = "https://hooks.slack.com/..."
